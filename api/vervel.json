{
    "buildCommand": "pip install -r requirements.txt",
    "outputDirectory": ".",
    "rewrites": [
        {
            "source": "(.*)",
            "destination": "/app.py/$1"
        }
    ]
}